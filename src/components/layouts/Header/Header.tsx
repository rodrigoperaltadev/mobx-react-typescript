import React, { FC } from 'react'

import './header.css'

const Header: FC = () => {
  return (
    <header id="header">
      <h3 className="header-text-title">Mobx React js</h3>
      <h5 className="header-text-subtitle">with Typescript</h5>
    </header>
  )
}

export default Header
